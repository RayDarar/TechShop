#pragma once

#define xf(n) for(int i = 0; i < n; i++)
#define xff(n) for(int i = n - 1; i >= 0; i--)
#define sp system("pause");
#define ss system("cls");

#include <iostream>
#include <conio.h>
#include <Windows.h>
#include <vector>
#include <string>
#include <fstream>
#include <sstream>
#include <list>
#include <algorithm>
#include <cstring>
#include <functional>

using namespace std;